#include <stdio.h>
int main()
{
	float a,sq,cb;
	printf("Enter the value of a :");
	scanf("%f",&a);
	sq=a*a;
	printf("Square Number%f=%f",a,sq);
	cb=a*a*a;
	printf("\ncube number %f=%f",a,cb);
}
